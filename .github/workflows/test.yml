name: test

on: pull_request

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Logging Test
        run: echo $GITHUB_REF_NAME
      env:
        GITHUB_REF_NAME: develop
