name: test

on:
  workflow_dispatch:

env:
  GITHUB_REF_NAME: develop

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: develop
      - name: Logging Test
        run: echo $GITHUB_REF_NAME
